@AGENTS.md

<!-- The real contract lives in AGENTS.md (read natively by Codex, Kimi
     Code, Cursor, Gemini CLI, Copilot, …). Claude Code pulls it in via
     the @-import above. Edit AGENTS.md, not this file — one instruction
     body, no drift. The import is used instead of a symlink so Windows
     checkouts with core.symlinks=false still work. -->
